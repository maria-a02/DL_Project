<%= form_for([@fair,@fair.opinions.build]) do |form| %>
    
    <div class="field">
        <%= form.label :Título %>
        <%= form.text_field :title %>
    </div>
    
    <div class="field">
        <%= form.label :Comentario %>
        <%= form.text_field :comment %>
    </div>
    
    <div class="actions">
        <%= form.submit class: 'btn btn-success' %>
    </div>

<% end %>