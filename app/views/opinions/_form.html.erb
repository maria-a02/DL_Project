<div class=formopinion>
    <%= form_with(model: [@fair, @opinion], local: true) do |form| %>
        
        <div class="field py-1 mx-3">
            <%= form.label :Título %>
            <%= form.text_field :title %>
        </div>
        
        <div class="field py-1 mx-3">
            <%= form.label :Comentario %>
            <%= form.text_area :comment, :cols => "50", :rows => "8"%>
        </div>
        
        <div class="actions py-1 mx-3">
            <%= form.submit class: 'btn btn-success' %>
        </div>

    <% end %>
</div>