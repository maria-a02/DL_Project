<h1>Opiniones</h1>

<table>
    <thead>
        <tr>
        <th>Título</th>
        <th>Comentario</th>
        <th>Rating</th>
        <th colspan="3"></th>
    </tr>
    </thead>
<tbody>
    <% @opinions.each do |apartment| %>
        <tr>
            <td><%= opinion.title%></td>
            <td><%= opinion.comment %></td>
            <td><%= opinion.rating %></td>
            <td><%= link_to 'Show', opinion %></td>
        </tr>
    <% end %>
</tbody>
</table>
<%= link_to 'Agregar comentario', new_opinion_path, class: 'btn btn-info' %>
<%= link_to 'Volver al inicio', root_path, class: 'btn btn-warning' %>
