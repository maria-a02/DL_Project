<nav class="navbar navbar-light fixed-top">
    <div class="container">
        <a class="navbar-brand" href="<%= root_path%>">
        <%= image_tag("logo.jpg", class: 'img-fluid rounded-circle')%>
        <span class="d-inline-block align-top text-nav">Ferias Libres de Santiago</span>
        </a>
        <div class="nav navbar-nav">
            <% if user_signed_in? %>
            <ul class="nav navbar-nav d-inline-block">
                <li class="nav-item active">
                    <%= link_to ' Perfil', profile_path(current_user.id), class:'btn btn-dark fas fa-user-alt 9x' %>
                    <%= link_to ' Comentarios', fair_opinions_path(current_user.id), class:'btn btn-dark fas fa-comments 9x' %>
                    <%= link_to ' Cerrar sesión', destroy_user_session_path, method: :delete, class:'btn btn-dark fas fa-sign-out-alt 9x' %>
                </li>   
            </ul>
            <% else %>
            <ul class="nav navbar-nav d-inline-block">
                <li class="nav-item active">
                    <%= link_to 'Registrarse', new_user_registration_path, class:'btn btn-success' %>
                    <%= link_to 'Iniciar sesión', new_user_session_path, class: 'btn btn-primary' %>
                </li> 
            </ul>
            <% end %>
        </div>
    </div>
</nav>