<%= form_with(model: @user, remote: true) do |form| %>  
    <% if @user.errors.any? %>    
        <div id="error_explanation">      
            <h2><%= pluralize(@user.errors.count, "error") %> no se pudo actualizar tu perfil</h2>
            <ul>      
            <% @user.errors.full_messages.each do |message| %>        
                <li><%= message %></li>      
            <% end %>      
            </ul>    
        </div>  
    <% end %>

    <div class="field py-1 m-3">    
        <%= form.label :Nombre %>    
        <%= form.text_field :name %>  
    </div>

    <div class="field py-1 m-3">    
        <%= form.label :Foto %>    
        <%= form.text_field :photo %>  
    </div>

        <div class="field py-1 m-3">    
        <%= form.label :Sobre_mí %>    
        <%= form.text_field :about %>  
    </div>

    <div class="actions py-1 m-3">    
    <%= form.submit class: 'btn btn-success' %>  
    </div>
<% end %>

<div class= 'py-2'>
    <%= link_to '', profile_path(current_user.id), class: 'btn btn-info fas fa-arrow-left py-1 m-3' %>
</div>